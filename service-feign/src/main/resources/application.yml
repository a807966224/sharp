server:
  port: 8930

spring:
  application:
    name: service-feign
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true
    rabbitmq:
      addresses: 10.220.115.21
      port: 5672
      username: guest
      password: guest

feign:
  hystrix:
    enabled: true
